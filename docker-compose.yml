services:
  goboxd:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: goboxd:dev
    container_name: goboxd
    ports:
      - "8080:8080"
    privileged: true
    cap_add:
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    restart: unless-stopped
    # Uncomment for faster development (live reload during coding)
    # volumes:
    #   - .:/app

  # Optional tools container for running tests, go commands, etc.
  tools:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    image: goboxd-tools:dev
    container_name: goboxd-tools
    volumes:
      - .:/src
    working_dir: /src
    profiles:
      - tools
    tty: true
    entrypoint: ["sleep", "infinity"]

# Optional: Global settings
networks:
  default:
    name: goboxd_network
